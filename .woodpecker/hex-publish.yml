---
# Hex publish pipeline — publishes the tailwind_builder ELIXIR LIBRARY to the
# private `defdo` Hex organization on tag push.
#
# This is distinct from the release-*/canary pipelines, which build the Tailwind
# CLI + DaisyUI standalone BINARIES and upload them to R2. This one publishes the
# mix package so consumers can depend on it via Hex instead of a local path.
#
# Secrets (global Woodpecker secrets, available on `tag` events):
#   hex_api_key    — Hex user API key with publish rights on the defdo org
#   hex_org_token  — Hex org read/auth key for the private `defdo` repo
#
# Trigger: pushing a version tag (e.g. `0.2.17`). The version comes from the
# VERSION file via mix.exs, so the tag must match it.
#
# Runs on the DOCKER/kubernetes agent (`platform: linux/amd64`), NOT the
# darwin/arm64 exec agent — that mac host is reserved for native macOS binary
# builds (release-macos-arm64.yml). Publishing a pure-Elixir library needs only a
# container with Elixir + Mix, so it belongs on the k8s agent. The label is
# REQUIRED: without it the tag job can land on the local (exec) agent, which tries
# to run the `image:` string as a host binary and dies with
# `exec: "<image>": executable file not found`.

labels:
  platform: linux/amd64

when:
  - event: tag

steps:
  - name: validate
    image: elixir:1.19.5
    environment:
      HEX_API_KEY:
        from_secret: hex_api_key
      HEX_ORG_TOKEN:
        from_secret: hex_org_token
    commands:
      - |
        set -e
        HEX_ORG_NAME="defdo"
        mix local.hex --force
        if [ -n "$HEX_ORG_TOKEN" ]; then
          mix hex.organization auth "$HEX_ORG_NAME" --key "$HEX_ORG_TOKEN"
        fi
        mix deps.get
        mix compile --warnings-as-errors
        mix hex.build

  - name: hex-publish
    image: elixir:1.19.5
    depends_on: [validate]
    environment:
      HEX_API_KEY:
        from_secret: hex_api_key
      HEX_ORG_TOKEN:
        from_secret: hex_org_token
    commands:
      - |
        set -e
        HEX_ORG_NAME="defdo"
        if [ -z "$HEX_API_KEY" ]; then
          echo "ERROR: HEX_API_KEY is required to publish"
          exit 1
        fi
        mix local.hex --force
        if [ -n "$HEX_ORG_TOKEN" ]; then
          mix hex.organization auth "$HEX_ORG_NAME" --key "$HEX_ORG_TOKEN"
        fi
        mix deps.get
        # Package and docs are published SEPARATELY on purpose. `mix hex.publish`
        # does both at once, and when its docs phase re-submits the release Hex
        # answers `must include the --replace flag to update an existing release`:
        # the package is live and the step still goes red. A red that does not mean
        # "not published" is worse than no signal — the next person reaches for
        # --replace and overwrites a published version.
        mix hex.publish package --yes
        mix hex.publish docs --yes
